# Review: Post-v0.10 Doc Tidy (v0.10.1)

## Parent Spec

`.ai/project/specs/2026-05-14-post-v0.10-doc-tidy/spec.md`

---

Spec: `.ai/project/specs/2026-05-14-post-v0.10-doc-tidy/spec.md`
Branch: `chore/runtime-post-v0.10-doc-tidy`
Commits: `60f0a85` (runtime + spec), `596ec1e` (README + ship
metadata), this review = C.

## Summary

Bundled cleanup of four doc-only follow-ups from v0.9.0 and
v0.10.0 reviews:

1. **Workflow cross-ref to INDEX § Traceability** — workflow
   doc now points readers at the canonical `## Parent <Type>`
   rules and `(none — <reason>)` rendering convention; also
   names `ai-runtime-kit validate` as the audit tool.
2. **Hot-fix example in task template** — `## Parent Plan`
   comment now shows the literal `(none — direct task)`
   rendering pattern.
3. **`spec-writer.md` Must Not bullet for metric reachability**
   — third class of self-rule promoted from review observation
   to agent file (alongside the size-budget rule). Pattern
   captured: when a spec asserts metrics on state that doesn't
   yet exist, mid-impl amendment becomes necessary.
4. **Walkthrough 3 per-step `## Parent <Type>` annotations** —
   each pipeline step now shows the structural upward-link
   field that anchors it. Closing line points at `validate`
   as the pre-merge audit step.

Second engineering-only spec on this kit (after v0.8.1).

## Verification

- `npm test` → **24/24 pass** (no test changes; doc-only).
- `node bin/cli.js validate` against this repo's tree:
  ```
  prds 3 / features 5 / specs 10 / plans 0 / tasks 0 / reviews 9
  Errors: none. Warnings: none. Result: PASS (clean tree).
  ```
  Validator self-test passes after the doc edits.
- `wc -c runtime/agents/spec-writer.md` → **1794 bytes**
  (under the spec's revised 1900 ceiling; 4th size-ceiling
  drift recorded in §Non-blocking Issues).

Runtime-scoped preflight (HOOK pre-executor/runtime-scoped-preflight):
  - Scope contract: PASS — 3 runtime paths.
  - Branch name: PASS — `chore/runtime-post-v0.10-doc-tidy`.
  - Spec home: PASS.

**Hook fire #8, eighth clean pass.** Zero GATE failures across
the entire session's governance ships.

## Acceptance Criteria

- [x] Workflow doc has cross-ref paragraph to INDEX §
      Traceability + `ai-runtime-kit validate` mention.
- [x] Task template's `## Parent Plan` comment shows the
      `(none — direct task)` literal example.
- [x] `spec-writer.md` has the new Must Not bullet; file
      ≤1900 bytes (1794).
- [x] README Walkthrough 3 has per-step `## Parent <Type>`
      annotations on each pipeline step.
- [x] All existing tests pass (24/24).
- [x] `validate` self-test passes on this repo's tree.

## Blocking Issues

None.

## Non-blocking Issues

- **Fourth size-ceiling drift** on `spec-writer.md`:
  prd-writer's ceiling was 1200 → 1500 (v0.5.1);
  prd-writer/agents-roster ceiling was 1500 → 1700 (v0.7.0);
  spec-writer was 1537 → 1680 (v0.8.1's bullet);
  spec-writer 1680 → 1794 (this release).
  Pattern is now firmly established: hard byte-ceilings on
  documentation files don't survive normal evolution.
  **Recorded as follow-up**: promote "spec budgets are ranges,
  not hard caps" from this pattern recognition to an actual
  workflow rule (probably in `runtime/agents/spec-writer.md`
  or `runtime/workflows/feature-development.md`). Likely a
  v0.11.0 candidate.
- **The new Must Not bullet's wording** is terse to fit under
  the ceiling: "Approve specs whose acceptance asserts metrics
  on state not yet in place (drift hit v0.5.1, v0.7.0,
  v0.10.0)." The version-list citations communicate the
  cumulative pattern but could lose freshness as those
  versions age. Watch-item; if a future reader doesn't
  recognize the version refs, the bullet's value drops.
- **`README.md` Walkthrough 3 length** is now ~85 lines —
  slightly above the v0.8.1 budget's upper bound of 80.
  Acceptable given the per-step annotations earn their
  bytes; watch-item if future expansion pushes it past 100.

## Suggested Fixes

- **Follow-up — promote "size budgets are ranges" to a rule**
  (v0.11.0?). Specifically: amend `runtime/agents/spec-writer.md`
  or add a workflow note saying "specs setting size budgets
  for new template/role files MUST express them as a range
  (e.g. 800-2000 bytes) rather than a hard cap." This would
  close the meta-pattern.
- **Carry forward (still pending):**
  - `validate --since/--diff` scoped validation (v0.10.0
    review).
  - `validate --check-downstream` (v0.10.0 review).
  - Externalize `EXCLUDED_FILENAMES` (v0.10.0 review).
  - Multi-agent entry files (v0.4.0 review's #2).
  - Commit-message conventions for task→commit link
    (general F4 follow-up).
- **Follow-up — author a real plan / task instance** to
  exercise the `## Parent Plan` rendering and the workflow's
  Plan/Task discipline in practice. The kit has implemented
  the artifact types but never actually created instances —
  the templates are unused outside `init`.

## Open Questions resolved

Minimal — all 4 spec-phase questions resolved during spec
drafting (workflow placement, template comment style,
bullet wording, README annotation style). No new open
questions raised during impl.

## Process notes (dogfood reflections)

- **Second engineering-only spec ships cleanly** (after
  v0.8.1). The streamlined no-PRD path is now well-trodden;
  the workflow correctly skips Steps 0 / 0.5 / 1.5 for
  doc-tidy work.
- **Eighth governance ship; eighth clean preflight pass.**
  Cumulative session: 8 fires, 0 GATE failures.
- **Validator now in the loop.** Pre- and post-commit
  validation is becoming routine — the doc edits in this
  release were sanity-checked via `validate` mid-impl. This
  is the dogfood of v0.10.0 paying dividends one release later.
- **4th drift on the same metric class** (size ceilings).
  Recognized as a pattern that needs structural fix, not
  another bullet. v0.11.0 candidate.
- **Run time**: ~10 minutes from spec → Commit C.
  Comparable to v0.8.1's record.

## Verdict

Approved. Ready to merge into `main`, push, tag `v0.10.1`,
optionally publish to npm. The lone meta-suggestion (range
budgets instead of hard caps) is recorded for v0.11.0.
