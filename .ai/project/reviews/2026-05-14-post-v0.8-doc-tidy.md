# Review: Post-v0.8 Doc Tidy (v0.8.1)

Spec: `.ai/project/specs/2026-05-14-post-v0.8-doc-tidy/spec.md`
Branch: `chore/runtime-post-v0.8-doc-tidy`
Commits: `cce9314` (runtime + spec APPROVED), `a1a82c9` (README +
ship metadata).

## Summary

Bundled cleanup of three carryover follow-ups from v0.7.0 and
v0.8.0 reviews:

1. **Architect grep audit** — 2 stale 5-phase pipeline mentions
   in `runtime/INDEX.md` (Hooks section + standalone
   Recommended Agent Flow) updated to the 8-phase chain. Other
   `architect` mentions correctly preserved (architecture-the-
   concept: ADRs, memory dirs, "architecture tree").
2. **`spec-writer.md` size-budget rule** — `## Must Not` bullet
   added recording the v0.5.1 + v0.7.0 size-ceiling drift
   lesson at the agent-self-rule level. Future spec authors
   read this and avoid the same trap.
3. **README Walkthrough 3** — full feature lifecycle (v0.8.0+)
   added showing the 8-phase PRD → review pipeline with
   concrete agent role invocations and file paths.

First **engineering-only spec** on the kit — no PRD/feature
parent. Streamlined no-PRD path through Step 1 (Define Spec)
to Step 2 (Execute) without Steps 0 or 0.5. Validates that the
workflow's "skip Step 0 for engineering-only" rule works in
practice.

## Verification

- `npm test` → **18/18 pass** (no test changes; only doc edits).
- `npm pack --dry-run` → 52 files / 52.1 kB. README grew
  ~3 kB; `spec-writer.md` 1537 → 1680 bytes (still ≤1700, the
  ceiling the new bullet describes — rule satisfies itself).
- Architect grep result post-impl: 6 remaining matches in
  `runtime/INDEX.md`, all legitimate (architecture-the-concept
  or the "architect replaced by spec-writer" explanatory
  note). 0 stale role-name references.

Runtime-scoped preflight (HOOK pre-executor/runtime-scoped-preflight):
  - Scope contract: PASS — listed paths: `runtime/INDEX.md`,
    `runtime/agents/spec-writer.md`.
  - Branch name: PASS — `chore/runtime-post-v0.8-doc-tidy`.
  - Spec home: PASS —
    `.ai/project/specs/2026-05-14-post-v0.8-doc-tidy/spec.md`.

Hook fire #6, sixth clean pass.

## Acceptance Criteria

- [x] `runtime/INDEX.md` Architect grep returns only legitimate
      architecture-concept matches (verified during impl).
- [x] `runtime/agents/spec-writer.md` ≤1700 bytes (1680 final).
- [x] `README.md` has `## Walkthrough 3` between Walkthrough 2
      and `## Local development`.
- [x] All existing tests pass (18/18).
- [x] No tarball size regression (52 files / 52.1 kB, modest
      increase consistent with the README addition).

## Blocking Issues

None.

## Non-blocking Issues

- **Walkthrough 3 length landed at ~70 lines**, near the upper
  bound of the spec's 50–80 line budget. Within range; no
  amendment needed. Future walkthroughs for additional kit
  features (F4 traceability, multi-agent entry, etc.) should
  budget similarly or split.
- **The size-budget Must Not bullet uses informal "v0.5.1 +
  v0.7.0" notation** rather than a path-reference. If a future
  reader wants the full lessons, they need to grep
  `.ai/project/reviews/` for those releases. Acceptable
  trade-off — keeping the bullet ≤150 chars to stay under the
  1700 byte ceiling — but worth a watch-item.

## Suggested Fixes

- **Carry forward to F4**: real TDD-applicable feature ship
  for M4 60-day metric (still pending — F4 itself is doc-only
  so won't trigger TDD).
- **Carry forward**: F4 (traceability link encoding) remains
  the last slice of the v0.6.0 nine-phase-workflow PRD.

## Open Questions resolved

The spec listed minimal open questions; all resolved during
spec drafting (architect grep scope pre-determined; walkthrough
placement chosen). No new open questions raised during impl.

## Process notes

- **First engineering-only spec**, validating the
  no-PRD/no-feature path. Spec → branch → impl → review
  completed in ~10 minutes — fastest pass yet, even faster
  than v0.8.0's 15-minute record (less ceremony, fewer
  artifacts to author).
- **Recursive rule satisfaction**: the size-budget Must Not
  bullet added in this release describes a rule that is
  itself satisfied by the release's own edit (spec-writer.md
  grew from 1537 to 1680 — under the 1700 ceiling that the
  new bullet warns against blowing).
- **Hook fire #6, sixth clean pass.** Six consecutive successes
  with zero GATE failures. The discipline is permanently
  internalized; the hook acts as a no-cost safety net rather
  than a friction point.
- **No new tests**, intentionally. The fixes are pure prose
  cleanup; structural tests for prose content would be
  test-for-test's-sake. Future tooling (F4-territory) could
  introduce link-resolution checks that would naturally cover
  the kinds of staleness this audit found.

## Verdict

Approved. Ready to merge into `main`, push, tag `v0.8.1`,
optionally publish to npm. F4 is the next planned slice;
real TDD-applicable feature ship for M4 measurement still
pending (F4 won't trigger it; needs a `src/**`-touching
feature).
